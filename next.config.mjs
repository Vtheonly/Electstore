/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'sxnrhruczgbjextrlcwq.supabase.co',
      'images.unsplash.com'
    ],
  },
};

export default nextConfig;